# x.wuh.site 工作流补充

> 工作流唯一入口为 shadow-dev-workflow，按阶段使用 `shadow-dev-propose`、`shadow-dev-apply`、`shadow-dev-review`、`shadow-dev-release` 和 `shadow-dev-archive`。

项目技术栈、命名和约束见根目录 `CLAUDE.md`；项目 active 执行真相见 `shadow-docs/knowledge/`，并通过 `shadow-docs/menu.md` 精确路由。冲突时项目级约束优先。
