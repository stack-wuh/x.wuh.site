# FootprintMap

足迹地图组件，展示地点标记和地图交互。

## 用法

```tsx
<FootprintMap footprints={footprints} variant='full' onMarkerClick={handleClick} />
```

## 说明

- 基于 maplibre-gl。
- `types.ts` 定义 `FootprintData` 和 `FootprintMapProps`。
